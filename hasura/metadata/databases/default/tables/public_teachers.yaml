table:
  name: teachers
  schema: public
array_relationships:
  - name: chats
    using:
      foreign_key_constraint_on:
        column: created_by
        table:
          name: chats
          schema: public
  - name: marks
    using:
      foreign_key_constraint_on:
        column: teacher_id
        table:
          name: marks
          schema: public
  - name: subjects
    using:
      foreign_key_constraint_on:
        column: teacher_id
        table:
          name: subjects
          schema: public
