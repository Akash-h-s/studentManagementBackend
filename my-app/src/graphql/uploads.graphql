
# upload info insertion
mutation InsertUploadInfo($filename: String!, $type: String!, $uploaded: Int!){
    insert_uploads_one(object: {
      filename: $filename
      type: $type
      uploaded_by: $uploaded
    }){
    id
  }
}

